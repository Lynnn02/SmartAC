<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Profile - SmartAC</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://unpkg.com/aos@next/dist/aos.css" />
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/theme-toggle.css">
    <meta name="theme-color" content="#000000">
    <style>
        body {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            background-color: var(--body-bg);
            color: var(--text-color);
            padding-top: 60px; /* Adjusted padding for fixed header */
        }
        
        /* Mobile optimizations */
        @media (max-width: 767.98px) {
            body {
                padding-bottom: 60px; /* Space for bottom nav */
            }
            
            .container {
                padding-left: 15px;
                padding-right: 15px;
            }
            
            h1.display-5 {
                font-size: 1.8rem;
                margin-bottom: 0.5rem;
            }
            
            p.lead {
                font-size: 1rem;
            }
            
            .profile-header {
                padding: 1.5rem 0;
            }
            
            .profile-header .py-4 {
                padding-top: 1rem !important;
                padding-bottom: 1rem !important;
            }
        }
        .profile-header {
            padding: 2rem 0;
            margin-top: 0;
            background: linear-gradient(135deg, rgba(49, 135, 123, 0.2), rgba(59, 95, 224, 0.2));
        }
        .card {
            border-radius: 16px;
            box-shadow: 0 10px 20px rgba(0, 202, 188, 0.08);
            border: none;
            overflow: hidden;
            background-color: white;
            color: #333;
            margin-bottom: 1.5rem;
        }
        
        /* Mobile optimizations for cards */
        @media (max-width: 767.98px) {
            .card {
                border-radius: 12px;
                box-shadow: 0 5px 15px rgba(0, 202, 188, 0.05);
                margin-bottom: 1rem;
            }
            
            .card-header {
                padding: 1rem 1.25rem;
            }
            
            .card-header h4 {
                font-size: 1.2rem;
            }
            
            .card-body {
                padding: 1.25rem;
            }
            
            .form-label {
                font-size: 0.9rem;
                margin-bottom: 0.3rem;
            }
            
            .form-control {
                font-size: 0.95rem;
                padding: 0.5rem 0.75rem;
                margin-bottom: 0.75rem;
            }
            
            .btn {
                padding: 0.5rem 1rem;
                font-size: 0.95rem;
            }
            
            .list-group-item {
                padding: 0.75rem 1.25rem;
                font-size: 0.9rem;
            }
        }
        .card-header {
            border-bottom: 1px solid rgba(0, 0, 0, 0.1);
            padding: 1.25rem 1.5rem;
            background-color: #f8f9fa;
            color: #333;
        }
        .card-body {
            padding: 1.5rem;
        }
        .list-group-item {
            background-color: white;
            border-color: rgba(0, 0, 0, 0.1);
            color: #333;
        }
        .user-avatar {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: linear-gradient(135deg, #2dd4bf, #3b5fe0);
            color: white;
            font-weight: bold;
        }
        .user-avatar.large {
            width: 100px;
            height: 100px;
            font-size: 2.5rem;
        }
        
        /* Mobile optimizations for user avatar and layout */
        @media (max-width: 767.98px) {
            .user-avatar.large {
                width: 80px;
                height: 80px;
                font-size: 2rem;
                margin-bottom: 0.5rem;
            }
            
            .section-divider {
                width: 50px;
                margin-bottom: 1rem;
            }
            
            /* Adjust layout for mobile */
            .row {
                margin-left: -10px;
                margin-right: -10px;
            }
            
            .col-lg-8, .col-lg-4 {
                padding-left: 10px;
                padding-right: 10px;
            }
            
            /* Ensure proper spacing at the bottom for fixed nav */
            .container.mb-5 {
                margin-bottom: 5rem !important;
            }
            
            /* Improve button styles for mobile */
            .btn-primary {
                background: linear-gradient(135deg, #2dd4bf, #3b5fe0);
                border: none;
                border-radius: 30px;
                font-weight: 500;
                padding: 0.6rem 1.2rem;
                box-shadow: 0 4px 10px rgba(59, 95, 224, 0.2);
                transition: all 0.3s ease;
            }
            
            .btn-primary:hover, .btn-primary:focus {
                transform: translateY(-2px);
                box-shadow: 0 6px 15px rgba(59, 95, 224, 0.3);
                background: linear-gradient(135deg, #2dd4bf, #3b5fe0);
                border: none;
            }
        }
        footer {
            margin-top: auto;
            background-color: var(--footer-bg);
            color: var(--footer-text);
            padding: 60px 0 30px;
        }
        .section-divider {
            height: 4px;
            width: 60px;
            background: linear-gradient(135deg, #2dd4bf, #3b5fe0);
            border-radius: 2px;
            margin: 0 auto 1.5rem;
        }
        [data-theme="dark"] .card {
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
        }
        .text-muted {
            color: #6c757d !important;
        }
        
        .text-primary {
            color: #3b5fe0 !important;
        }
        
        .text-success {
            color: #2dd4bf !important;
        }
    </style>
</head>
<body>
    <div class="container-fluid p-0 footer-wrapper">
        <!-- Include student header partial -->
        <%- include('../partials/student-header', { user: user, activePage: 'profile' }) %>

    <section class="profile-header py-4 text-center d-flex align-items-center">
        <div class="container py-4">
            <div class="row align-items-center">
                <div class="col-lg-8 mx-auto text-center">
                    <div class="user-avatar large mx-auto mb-3">
                        <span><%= user.name.charAt(0) %></span>
                    </div>
                    <h1 class="display-5 fw-bold mb-3" style="color: #333;"><%= user.name %></h1>
                    <div class="section-divider mb-3 mx-auto"></div>
                    <p class="lead mb-0" style="color: #333;">Student Profile</p>
                </div>
            </div>
        </div>
    </section>

    <div class="container mb-5">
        <div class="row">
            <div class="col-lg-8">
                <div class="card shadow-sm mb-4" data-aos="fade-up" data-aos-duration="800">
                    <div class="card-header">
                        <h4 class="mb-0 fw-bold">Profile Information</h4>
                    </div>
                    <div class="card-body">
                        <form action="/student/profile/update" method="POST">
                            <div class="mb-3">
                                <label for="name" class="form-label">Name</label>
                                <input type="text" class="form-control" id="name" name="name" value="<%= user.name %>" required>
                            </div>
                            <div class="mb-3">
                                <label for="username" class="form-label">Username</label>
                                <input type="text" class="form-control" id="username" name="username" value="<%= user.username %>" required>
                            </div>
                            <div class="mb-3">
                                <label for="email" class="form-label">Email</label>
                                <input type="email" class="form-control" id="email" name="email" value="student@smartac.com">
                            </div>
                            <div class="mb-3">
                                <label for="studentId" class="form-label">Student ID</label>
                                <input type="text" class="form-control" id="studentId" name="studentId" value="TVET-2025-001">
                            </div>
                            <div class="d-grid">
                                <button type="submit" class="btn btn-primary">Update Profile</button>
                            </div>
                        </form>
                    </div>
                </div>

                <div class="card shadow-sm" data-aos="fade-up" data-aos-duration="800" data-aos-delay="100">
                    <div class="card-header">
                        <h4 class="mb-0 fw-bold">Change Password</h4>
                    </div>
                    <div class="card-body">
                        <form action="/student/profile/change-password" method="POST">
                            <div class="mb-3">
                                <label for="currentPassword" class="form-label">Current Password</label>
                                <input type="password" class="form-control" id="currentPassword" name="currentPassword" required>
                            </div>
                            <div class="mb-3">
                                <label for="newPassword" class="form-label">New Password</label>
                                <input type="password" class="form-control" id="newPassword" name="newPassword" required>
                            </div>
                            <div class="mb-3">
                                <label for="confirmPassword" class="form-label">Confirm New Password</label>
                                <input type="password" class="form-control" id="confirmPassword" name="confirmPassword" required>
                            </div>
                            <div class="d-grid">
                                <button type="submit" class="btn btn-primary">Change Password</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
            <div class="col-lg-4">
                <div class="card shadow-sm mb-4" data-aos="fade-up" data-aos-duration="800">
                    <div class="card-header">
                        <h4 class="mb-0 fw-bold">Account Information</h4>
                    </div>
                    <div class="card-body">
                        <ul class="list-group list-group-flush">
                            <li class="list-group-item d-flex justify-content-between align-items-center">
                                <span>Role</span>
                                <span class="badge bg-success">Student</span>
                            </li>
                            <li class="list-group-item d-flex justify-content-between align-items-center">
                                <span>Account ID</span>
                                <span class="text-muted">#<%= user.id %></span>
                            </li>
                            <li class="list-group-item d-flex justify-content-between align-items-center">
                                <span>Last Login</span>
                                <span class="text-muted">Today, 6:00 AM</span>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Include student footer partial -->
    <%- include('../partials/student-footer') %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://unpkg.com/aos@next/dist/aos.js"></script>
    <script src="/js/theme-toggle.js"></script>
    
    <script>
        // Initialize AOS animations
        AOS.init();
        
        document.addEventListener('DOMContentLoaded', function() {
            // Add padding to body for fixed header
            document.body.style.paddingTop = document.querySelector('header').offsetHeight + 'px';
        });
    </script>
</body>
</html>
