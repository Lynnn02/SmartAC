<!-- Student Header Partial -->
<header class="fixed-top">
    <nav class="navbar navbar-expand-lg navbar-dark py-2">
        <div class="container-fluid px-4">
            <a class="navbar-brand d-flex align-items-center" href="/student/dashboard">
                <span class="fw-bold fs-2 logo-text">SmartAC</span>
                <span class="ms-2 text-dark">&nbsp Student</span>
            </a>
        
        <!-- Theme toggle and user profile for mobile -->
        <div class="d-flex align-items-center order-lg-2">
            <!-- Theme toggle will be added here by JavaScript -->
            <div class="theme-toggle-container me-2 me-lg-3"></div>
            
            <div class="dropdown d-inline-block">
                <a class="nav-link dropdown-toggle d-flex align-items-center p-0" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    <div class="user-avatar me-md-2">
                        <span><%= user.name.charAt(0) %></span>
                    </div>
                    <span class="d-none d-md-inline-block"><%= user.name %></span>
                </a>
                <ul class="dropdown-menu dropdown-menu-end shadow-sm" aria-labelledby="userDropdown">
                    <li><a class="dropdown-item" href="/student/profile"><i class="bi bi-person me-2"></i>Profile</a></li>
                    <li><hr class="dropdown-divider"></li>
                    <li><a class="dropdown-item" href="/logout"><i class="bi bi-box-arrow-right me-2 text-danger"></i>Logout</a></li>
                </ul>
            </div>
            
            <button class="navbar-toggler ms-2" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
        </div>
        
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav mx-auto">
                <li class="nav-item">
                    <a class="nav-link <%= activePage === 'dashboard' ? 'active' : '' %> px-3" href="/student/dashboard">
                        <i class="bi bi-speedometer2 me-2"></i> Home
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= activePage === 'join-session' || activePage === 'session-detail' ? 'active' : '' %> px-3" href="/student/join-session">
                        <i class="bi bi-people-fill me-2"></i> Sessions
                    </a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle <%= activePage === 'practical' ? 'active' : '' %> px-3" href="#" id="practicalDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="bi bi-journal-text me-2"></i> Practical Modules
                    </a>
                    <ul class="dropdown-menu shadow-sm" aria-labelledby="practicalDropdown">
                        <li><a class="dropdown-item" href="/student/practical/1">Praktikal 1</a></li>
                        <li><a class="dropdown-item" href="/student/practical/2">Praktikal 2</a></li>
                        <li><a class="dropdown-item" href="/student/practical/3">Praktikal 3</a></li>
                    </ul>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= activePage === 'components' ? 'active' : '' %> px-3" href="/student/components">
                        <i class="bi bi-grid-3x3-gap me-2"></i> AC Components
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>
</header>

<style>
            body {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            background-color: var(--body-bg);
            color: var(--text-color);
        }
        .dashboard-header {
            background: linear-gradient(135deg, #2dd4bf, #3b5fe0);
            color: white;
            padding: 2.5rem 0;
            margin-bottom: 2rem;
            position: relative;
            overflow: hidden;
        }
        .dashboard-header::before {
            content: '';
            position: absolute;
            top: -50px;
            right: -50px;
            width: 200px;
            height: 200px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.1);
        }
        .dashboard-header::after {
            content: '';
            position: absolute;
            bottom: -80px;
            left: -80px;
            width: 180px;
            height: 180px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.1);
        }
        .dashboard-header .container {
            position: relative;
            z-index: 1;
        }
        .modern-card {
            border-radius: 16px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.08);
            transition: all 0.3s ease;
            border: none;
            overflow: hidden;
            height: 100%;
            background-color: var(--card-bg-color);
            color: var(--text-color);
        }
        
        [data-theme="dark"] .modern-card {
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
        }
        
        [data-theme="dark"] .btn-light {
            background-color: rgba(255, 255, 255, 0.1);
            color: var(--text-color);
            border-color: transparent;
        }
        
        [data-theme="dark"] .btn-light:hover {
            background-color: rgba(255, 255, 255, 0.2);
            color: #fff;
        }
        .modern-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.12);
        }
        .card-icon {
            width: 60px;
            height: 60px;
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.8rem;
            margin-bottom: 1rem;
            background: rgba(59, 95, 224, 0.1);
            color: #3b5fe0;
        }
        .blue-card .card-icon {
            background: rgba(59, 95, 224, 0.1);
            color: #3b5fe0;
        }
        .green-card .card-icon {
            background: rgba(45, 212, 191, 0.1);
            color: #2dd4bf;
        }
        .yellow-card .card-icon {
            background: rgba(255, 170, 0, 0.1);
            color: #ffaa00;
        }
        .pink-card .card-icon {
            background: rgba(236, 72, 153, 0.1);
            color: #ec4899;
        }
        .section-title {
            position: relative;
            margin-bottom: 1.5rem;
            padding-bottom: 0.5rem;
        }
        .section-title::after {
            content: '';
            position: absolute;
            left: 0;
            bottom: 0;
            width: 50px;
            height: 3px;
            background: linear-gradient(135deg, #2dd4bf, #3b5fe0);
            border-radius: 3px;
        }
        .btn-primary {
            background: linear-gradient(135deg, #2dd4bf, #3b5fe0);
            border: none;
            box-shadow: 0 4px 15px rgba(59, 95, 224, 0.3);
            transition: all 0.3s ease;
        }
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(59, 95, 224, 0.4);
        }
        .navbar {
            background-color: var(--navbar-bg) !important;
        }
        .user-avatar {
            width: 36px;
            height: 36px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            background: rgba(255, 255, 255, 0.2);
            color: white;
            font-weight: 600;
        }
        .session-card {
            border-radius: 16px;
            overflow: hidden;
            transition: all 0.3s ease;
            border: 1px solid var(--border-color);
            background-color: var(--card-bg-color);
        }
        .session-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.1);
        }
        footer {
            margin-top: auto;
            background-color: var(--footer-bg);
            color: var(--footer-text);
            padding: 60px 0 30px;
        }
        .footer-logo {
            font-weight: 700;
            margin-bottom: 1rem;
            font-size: 1.5rem;
        }
        .footer-desc {
            opacity: 0.8;
            font-size: 0.9rem;
            margin-bottom: 1.5rem;
        }
        .footer-links {
            list-style: none;
            padding: 0;
            margin: 0;
        }
        .footer-links li {
            margin-bottom: 0.75rem;
        }
        .footer-links a {
            color: inherit;
            text-decoration: none;
            transition: all 0.3s ease;
            opacity: 0.8;
        }
        .footer-links a:hover {
            opacity: 1;
            color: var(--primary-color);
        }
        .social-links a {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 36px;
            height: 36px;
            border-radius: 50%;
            background-color: rgba(255, 255, 255, 0.1);
            color: inherit;
            margin-right: 0.5rem;
            transition: all 0.3s ease;
        }
        .social-links a:hover {
            background-color: var(--primary-color);
            color: #fff;
            transform: translateY(-3px);
        }
        .contact-item {
            display: flex;
            margin-bottom: 1rem;
        }
        .contact-icon {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background-color: rgba(255, 255, 255, 0.1);
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 1rem;
        }
        .contact-link {
            color: inherit;
            text-decoration: none;
            opacity: 0.8;
            transition: all 0.3s ease;
        }
        .contact-link:hover {
            opacity: 1;
            color: var(--primary-color);
        }
        
        /* Component styles */
        .feature-list {
            list-style: none;
            padding: 0;
            margin: 0;
        }
        
        .feature-list li {
            margin-bottom: 0.75rem;
            display: flex;
            align-items: center;
            color: var(--text-color);
        }
        
        .component-icon-large {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            background: linear-gradient(135deg, rgba(45, 212, 191, 0.2), rgba(59, 95, 224, 0.2));
            color: var(--primary-color);
            margin-bottom: 1.5rem;
        }
        
        /* Responsive adjustments */
        @media (max-width: 768px) {
            .dashboard-header {
                padding: 2rem 0;
            }
            .card-icon {
                width: 50px;
                height: 50px;
                font-size: 1.5rem;
            }
            .section-title {
                font-size: 1.5rem;
            }
        }
        
        @media (max-width: 576px) {
            .dashboard-header {
                padding: 1.5rem 0;
            }
            .dashboard-header h1 {
                font-size: 1.8rem;
            }
            .dashboard-header p {
                font-size: 1rem;
            }
        }
    .avatar-circle {
        width: 32px;
        height: 32px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: bold;
    }
    
    .nav-link.active {
        color: #3b5fe0 !important;
        font-weight: 500;
    }
</style>
